# ComfyUI-Video-Chunker-GPU
English:  A Python wrapper for ComfyUI to process long videos via chunking and parallel execution, preventing OOM crashes.  日本語:  ComfyUIで長尺動画を分割・並列処理し、メモリ不足（OOM）を回避するPythonラッパースクリプト。
